# Navigation Layer (Discrete)

This module implements a discrete navigation strategy
on state graphs.

It includes:

- regime-based scoring
- risk distance computation (BFS)
- lookahead evaluation
- policy-based next-state selection

⚠️ Current status:

- requires a state graph adapter
- not yet integrated with FIELD layer

👉 Acts as a prototype decision engine
for NEXAH navigation.
